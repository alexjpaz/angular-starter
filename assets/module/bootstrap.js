/**
 * @file Bootstrap function to attach angularjs to the document and start the application
 * @author apaz
 * @depracated use the tag
*/
angular.element(document).ready(function() {
	angular.bootstrap(document.documentElement, ['vehimatics']);
});
/** @file */
